IconButton — square icon-only button (uses Lucide icons), for compact toolbars.

```jsx
<IconButton icon={<i data-lucide="bell"/>} label="Notifications" />
```

Note: pair with a `Tooltip` since it has no visible text label.
