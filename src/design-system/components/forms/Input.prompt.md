Input — labeled single-line text field with optional help text or validation error.

```jsx
<Input label="Email" placeholder="you@example.com" />
<Input label="Password" type="password" error="Password is required" />
```
